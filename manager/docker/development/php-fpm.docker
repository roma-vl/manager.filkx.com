FROM php:8.3-fpm

RUN apt-get update \
  && apt-get install -y \
    libpq-dev \
    zlib1g-dev \
    zip \
    libzip-dev \
  && docker-php-ext-configure zip \
  && docker-php-ext-install zip pdo pdo_pgsql

WORKDIR /app
